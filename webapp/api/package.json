{
  "name": "investpulse-api",
  "version": "1.0.0",
  "description": "Azure Functions API for InvestPulse - Finnhub.io upstream proxy",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.27",
    "typescript": "^5.0.0"
  }
}
