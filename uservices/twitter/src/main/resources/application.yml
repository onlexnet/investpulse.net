server:
  port: 9001

spring:
  application:
    name: twiter-consumer

  
  config:
    import: optional:configserver:http://localhost:8888

  
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      group-id: twiter-consumer  # Consumer group for Spring Cloud Bus
      auto-offset-reset: earliest
          
  cloud:
    bus:
      id: twiter-consumer-${random.int:1000}  # ← KLUCZOWE: stały ID grupy!
      enabled: true
      destination: springCloudBus  # Kafka topic name for config events
      refresh:
        enabled: true  # Auto-refresh @RefreshScope beans on bus events
    stream:
      bindings:
        springCloudBusInput:
          destination: springCloudBus
          group: moja-grupa


management:
  endpoints:
    web:
      exposure:
        include: busrefresh,refresh,health,info
      base-path: /actuator
  endpoint:
    refresh:
      enabled: true

twitter:
  bearer-token: UNDEFINED
